<%= form_for(@test_distrubuted_build) do |f| %>
  <% if @test_distrubuted_build.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@test_distrubuted_build.errors.count, "error") %> prohibited this test_distrubuted_build from being saved:</h2>

      <ul>
      <% @test_distrubuted_build.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
